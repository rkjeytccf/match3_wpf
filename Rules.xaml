<Page x:Class="finalKursach.Rules"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        d:DesignHeight="1080" d:DesignWidth="1920"
        Title="Rules" >
    <Grid>
        <Border Background="#99000000">
            <Grid>
                <Border BorderBrush="LightPink" BorderThickness="4" CornerRadius="8" Width="500" Height="380" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel Background="WhiteSmoke" >
                        <Image Source="\images\off.png" Panel.ZIndex="1" Width="50" Height="50" Margin="5" HorizontalAlignment="Right" VerticalAlignment="Top" MouseLeftButtonDown="BackImage_MouseLeftButtonDown"/>
                        <Image Source="\rules.jpg" Stretch="Fill"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Page>
